class LootItem {
    constructor({ itemId, dropPercentage, isDefaultDrop }) {
        this.itemId = itemId;
        this.dropPercentage = dropPercentage;
        this.isDefaultDrop = isDefaultDrop;
    }
}